ConfigD = {}

ConfigD[1] = {
    _id= 1,
    map = 1, --此id对应MapGroupConfig中配置文件，填0表示无
    gap = 100,    --每组之间的间隔差距，单位:像素
    probability = 60,    --权重
}
ConfigD[2] = {
    _id= 2,
    map = 1, --此id对应MapGroupConfig中配置文件，填0表示无
    gap = 100,    --每组之间的间隔差距，单位:像素
    probability = 60,    --权重
}
ConfigD[3] = {
    _id= 3,
    map = 1, --此id对应MapGroupConfig中配置文件，填0表示无
    gap = 100,    --每组之间的间隔差距，单位:像素
    probability = 60,    --权重
}
ConfigD[4] = {
    _id= 4,
    map = 1, --此id对应MapGroupConfig中配置文件，填0表示无
    gap = 100,    --每组之间的间隔差距，单位:像素
    probability = 60,    --权重
}
ConfigD[5] = {
    _id= 5,
    map = 1, --此id对应MapGroupConfig中配置文件，填0表示无
    gap = 100,    --每组之间的间隔差距，单位:像素
    probability = 60,    --权重
}
ConfigD[6] = {
    _id= 6,
    map = 1, --此id对应MapGroupConfig中配置文件，填0表示无
    gap = 100,    --每组之间的间隔差距，单位:像素
    probability = 60,    --权重
}
ConfigD[7] = {
    _id= 7,
    map = 1, --此id对应MapGroupConfig中配置文件，填0表示无
    gap = 100,    --每组之间的间隔差距，单位:像素
    probability = 200,    --权重
}
ConfigD[8] = {
    _id= 8,
    map = 1, --此id对应MapGroupConfig中配置文件，填0表示无
    gap = 100,    --每组之间的间隔差距，单位:像素
    probability = 60,    --权重
}
ConfigD[9] = {
    _id= 9,
    map = 1, --此id对应MapGroupConfig中配置文件，填0表示无
    gap = 100,    --每组之间的间隔差距，单位:像素
    probability = 70,    --权重
}
ConfigD[10] = {
    _id= 10,
    map = 1, --此id对应MapGroupConfig中配置文件，填0表示无
    gap = 100,    --每组之间的间隔差距，单位:像素
    probability = 60,    --权重
}
ConfigD[11] = {
    _id= 11,
    map = 1, --此id对应MapGroupConfig中配置文件，填0表示无
    gap = 100,    --每组之间的间隔差距，单位:像素
    probability = 20,    --权重
}
ConfigD[12] = {
    _id= 12,
    map = 1, --此id对应MapGroupConfig中配置文件，填0表示无
    gap = 10,    --每组之间的间隔差距，单位:像素
    probability = 200,    --权重
}
ConfigD[13] = {
    _id= 13,
    map = 1, --此id对应MapGroupConfig中配置文件，填0表示无
    gap = 10,    --每组之间的间隔差距，单位:像素
    probability = 120,    --权重
}